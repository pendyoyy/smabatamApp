<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sman1batam">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SMAN1BATAM"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.ActivityJadwalUjian" />
        <activity android:name=".View.ViewJadwalUjian" />
        <activity android:name=".View.ViewJadwalHarian" />
        <activity android:name=".activity.ActivityJadwal" />
        <activity android:name=".users.RegistrasiActivity" />
        <activity android:name=".siswa.HomeSiswa" />
        <activity android:name=".guru.HomeGuru" />
        <activity android:name=".MainActivity" />
        <activity android:name=".users.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>